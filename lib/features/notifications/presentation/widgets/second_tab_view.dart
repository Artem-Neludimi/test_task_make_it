import 'package:flutter/material.dart';

class SecondTabView extends StatelessWidget {
  const SecondTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
