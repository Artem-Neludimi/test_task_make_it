import 'package:flutter/material.dart';

class CreatingNotificationScreen extends StatelessWidget {
  const CreatingNotificationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
